# jrm-reels

Public staging host for Jesse Rich Ministries reels.

Instagram's Content Publishing API does not accept a file upload — it requires a
public HTTPS `video_url` that Meta's servers fetch. GitHub Pages serves that URL.

Used by `~/jrm_autopost/jrm_meta_post.py`. Files land in `reels/`.
